# `bap` Homebrew Formulae
📦 The Homebrew repo for the bap CLI tool.

## Installing

To install using Homebrew:

```sh
brew tap mcncl/bap
brew install bap
```

## Usage

See the [bap](https://github.com/mcncl/bap) repo for information on the tool itself.
